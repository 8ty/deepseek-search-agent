# 🚫 已禁用 - 请使用 search_agent.yml (🔍 DeepSeek 搜索代理)
# 此工作流已被新版本替代，保留仅供参考
name: "[已禁用] DeepSeek Search Agent (旧版本)"

# 禁用此工作流 - 注释掉触发器
# on:
#   repository_dispatch:
#     types: [search-request]

# 保留原始配置供参考，但已禁用
on: []  # 空的触发器数组，确保此工作流不会被触发

jobs:
  run-search-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Run search agent with detailed logging
        env:
          QUERY: ${{ github.event.client_payload.query }}
          CALLBACK_URL: ${{ github.event.client_payload.callback_url }}
          JINA_API_KEY: ${{ secrets.JINA_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python -m api.github_runner